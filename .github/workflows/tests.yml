name: CI Frontend Móvil - San Felipe Inmobiliaria

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  # ======================================================
  # JOB 1: VALIDACIÓN DEL FRONTEND (React Native + Expo)
  # Objetivo: Instalar deps y verificar la sintaxis de TypeScript.
  # ======================================================
  frontend_ci:
    name: Frontend - Instalación y Tipado (Movil)
    runs-on: ubuntu-latest
    
    steps:
      - name: 📦 Checkout del código
        uses: actions/checkout@v4

      - name: 🟢 Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: ⚙️ Instalar dependencias del Movil
        # Se ejecuta dentro de la carpeta del proyecto móvil
        run: cd Movil-Inmobiliaria && npm install

      - name: 🕵️‍♀️ Verificar Tipado del Frontend
        # npx tsc --noEmit verifica que todo el código TS esté correcto sin generar archivos.
        run: cd Movil-Inmobiliaria && npx tsc --noEmit
        
      # NOTA: Se han quitado 'npm test', 'npm run lint' y cualquier paso nativo.